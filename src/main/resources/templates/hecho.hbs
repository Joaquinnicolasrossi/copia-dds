{{#partial "contenido"}}
    <div class="hecho-container">
        <a href="/hechos">← Volver</a>
        <div class="hecho-div">
            <div class="hecho-info">
                <h3 class="hecho-titulo">{{titulo}}</h3>
                <p class="hecho-fecha">{{fecha}}</p>
                <p class="hecho-descripcion">{{descripcion}}</p>
            </div>

            <div class="hecho-galeria">
                {{#if multimedia}}
                    {{#each multimedia}}
                        <div class="hecho-imagen">
                            {{#if this.esVideo}}
                                <video class="hecho-img" controls style="max-width: 100%; height: auto;">
                                    <source src="/{{this.url}}" type="{{this.tipo}}">
                                </video>
                            {{else}}
                                <img class="hecho-img" src="/{{this.url}}" alt="Imagen del hecho" style="max-width: 100%; height: auto;">
                            {{/if}}
                        </div>
                    {{/each}}
                {{else}}
                    <p>Este hecho no tiene archivos multimedia.</p>
                {{/if}}
            </div>

            <div>
                <form action="/solicitud/{{id}}/form" method="get">
                    <button type="submit">Solicitar eliminación</button>
                </form>
            </div>
        </div>
    </div>
{{/partial}}

{{> templates/layout }}